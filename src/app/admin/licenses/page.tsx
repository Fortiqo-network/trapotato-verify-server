import LicensesClient from './LicensesClient';

export const dynamic = 'force-dynamic';
export const metadata = { title: 'Licenses — Trapotato' };

export default function LicensesPage() {
  return <LicensesClient />;
}
