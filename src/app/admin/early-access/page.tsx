import EarlyAccessClient from './EarlyAccessClient';

export const dynamic = 'force-dynamic';
export const metadata = { title: 'Early Access — Trapotato' };

export default function EarlyAccessPage() {
  return <EarlyAccessClient />;
}
