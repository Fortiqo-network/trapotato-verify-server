import DeletedClient from './DeletedClient';

export const dynamic = 'force-dynamic';
export const metadata = { title: 'Deleted Users — Trapotato' };

export default function DeletedPage() {
  return <DeletedClient />;
}
